version: '2'
services:
  backendContainer:
    build:
      context: .
      args:
        FastAPIENV: "production"

    image: popopopony/peak-end_backend:v1

    container_name: backendContainer

    ports: 
    - "8080:8080"

    env_file:
    - .env